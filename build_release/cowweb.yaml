apiVersion: v1
kind: Service
metadata:
  name: cowweb
  namespace: cowweb
spec:
  type: ClusterIP
  selector:
    app: cowweb
  ports:
  - name: http
    port: 80
    targetPort: 8080
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: cowweb
  namespace: cowweb
  labels:
    app: cowweb
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cowweb
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cowweb
    spec:
      containers:
      - name: cowweb
        image: docker.io/hhayakaw/cowweb
        ports:
        - name: api
          containerPort: 8080
        readinessProbe:
          httpGet:
            path: /cowsay/ping
            port: api
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /cowsay/ping
            port: api
          initialDelaySeconds: 15
          periodSeconds: 20
